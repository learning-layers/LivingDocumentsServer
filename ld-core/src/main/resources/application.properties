# ------------------------------------------
# SERVER CONFIGURATION
# ------------------------------------------
server.port = 9000
management.port = 9001
logging.file = data/log.out

# ------------------------------------------
# CORE MODULE
# ------------------------------------------

# E-Mail
email.host = smtp.gmail.com
email.port = 587
email.username = <email>
email.password = <pass>
email.smtp.auth = true
email.smtp.starttls.enable =  true
mail.smtp.ssl.trust = true
email.from.system = <from>

# Security
#module.core.security.allowedOrigin = http://localhost:63342

# Content repository
module.core.repository.home = ./data/jackrabbit
module.core.repository.password = 1a1cbc2a-0b4a-48df-9903-dc4d3d9b5dff

# Database Configuration

# H2
#module.core.db.driver = org.h2.Driver
#module.core.db.dialect = org.hibernate.dialect.H2Dialect
#module.core.db.url = jdbc:h2:file:~/database/livingdocumentsdb;MVCC=True
#module.core.db.username = sa
#module.core.db.password =
#module.core.db.ddl = create-drop
#module.core.db.log.sql = true

# MySQL
module.core.db.driver = com.mysql.jdbc.Driver
module.core.db.dialect = org.hibernate.dialect.MySQLDialect
module.core.db.url = jdbc:mysql://localhost:3306/livingdocumentsdb
module.core.db.username = root
module.core.db.password =
module.core.db.ddl = create-drop
module.core.db.log.sql = true

# Postgres
#module.core.db.driver = org.postgresql.Driver
#module.core.db.dialect = org.hibernate.dialect.PostgreSQLDialect
#module.core.db.url = jdbc:postgresql://localhost:5432/livingdocumentsdb
#module.core.db.username = postgres
#module.core.db.password = postgres
#module.core.db.ddl = create-drop
#module.core.db.log.sql = true

# Authentication (internal (default), ldap)
module.core.auth.method = internal

# LDAP configuration
module.core.auth.ldap.url = ldap://localhost:10389
module.core.auth.ldap.base = dc=springframework,dc=org
module.core.auth.ldap.systemUserDn = uid=admin,ou=system
module.core.auth.ldap.systemUserPassword = secret
module.core.auth.ldap.userDnPatterns = uid={0},ou=people
module.core.auth.ldap.groupSearchBase = ou=groups
module.core.auth.ldap.fullNameAttribute = cn
module.core.auth.ldap.groupRoleMapping = Admin:ROLE_ADMIN;Student:ROLE_STUDENT

# Administrator password
module.core.bootstrap.admin.password = pass

# ------------------------------------------
# SANDBOX MODULE
# ------------------------------------------
module.sandbox.enabled = true
module.sandbox.password = pass
module.sandbox.numberOfUsers = 100
module.sandbox.users = Martin:Bachl:mbachl;Dennis:Soehnen:dsoehnen
